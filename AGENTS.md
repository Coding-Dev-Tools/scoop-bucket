# Repo instructions

Maintain Scoop manifests under `bucket/`. Each manifest should include `name`, `version`, `description`, `homepage`, `license`, `depends`, `url`, `hash`, and `post_install`. Keep diffs minimal, validate JSON, and do not add non-manifest files outside `bucket/` without review.
